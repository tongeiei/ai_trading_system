# copy this file to .env and fill in real values — .env is gitignored, never commit it

BINANCE_TESTNET_API_KEY=
BINANCE_TESTNET_API_SECRET=

# mainnet key: create later, permission = trade-only, withdraw disabled
BINANCE_MAINNET_API_KEY=
BINANCE_MAINNET_API_SECRET=

# --- XAU track: runtime LLM (P6 AI layer) ---
# Only the key for config/xau.yaml's `llm.active` provider is required.
# Kimi/Moonshot global console: https://platform.moonshot.ai (API Keys -> Create API Key)
# The account needs a prepaid balance before the key will serve requests.
MOONSHOT_API_KEY=
# DEEPSEEK_API_KEY=
# OPENAI_API_KEY=
