# secrets — never commit
.env
*.env
!.env.example
*.key
*.pem
*.pub

# python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/

# data / artifacts
*.parquet
*.db
*.sqlite
# root-level build artifacts ONLY. Must not be a bare `artifacts/`: that also
# swallowed docs/research/artifacts/, which holds the raw evidence behind every
# FALSIFIED verdict in GOLD_HANDOFF.md — 23 files that had never been committed.
/artifacts/
data/raw/
data/processed/

# os / editor
.DS_Store
.vscode/
.idea/
